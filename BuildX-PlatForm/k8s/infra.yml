apiVersion: v1
kind: Service
metadata:
  name: minio-service
  namespace: buildx-apps
spec:
  type: ExternalName
  externalName: host.docker.internal
  ports:
    - port: 9000
      targetPort: 9000
      protocol: TCP